# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Service dirs
.idea/
*.db
*.db-wal
*.db-shm

# Локальные данные и инструкции агента
/data/
PROMT.md
tg-mcp-spy.container

# uv lock не используем (ставим через pip)
uv.lock

# Редакторные swap-файлы
*.swp

# Локальные файлы, не публикуемые в форк
run.sh
HERMES.md

screenshots/
Screenshot*

# Локальные бэкапы веб-интерфейса (не публиковать в форк)
*.html.bak

# Локальные файлы сервера (не публиковать в форк):
#  - restart.sh — скрипт перезапуска/обновления контейнера,
#  - data-8090/ — volume с БД ( telegram_cache.db ), живёт на хосте.
restart.sh
data-8090/
